query RepoView($query: String!) {
  search(query: $query, type: REPOSITORY, first: 100) {
    nodes {
      __typename
      ... on Repository {
        nameWithOwner
        stargazers {
          totalCount
        }
      }
    }
  }
}
