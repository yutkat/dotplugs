query RepoView($query: String!, $first: Int!) {
  search(query: $query, type: REPOSITORY, first: $first) {
    nodes {
      __typename
      ... on Repository {
        nameWithOwner
        stargazers {
          totalCount
        }
      }
    }
    pageInfo {
      hasNextPage
    }
  }
}
